% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_predawn.R
\name{filter_predawn}
\alias{filter_predawn}
\title{Filter Data Frame to Predawn Hours}
\usage{
filter_predawn(data, window, mode = "static", dawn_times = NULL, tz = NULL)
}
\arguments{
\item{data}{A data frame with a \code{datetime} (POSIXct) column.}

\item{window}{Numeric vector of length 2. See \code{\link{resolve_predawn_hours}}.}

\item{mode}{Character, \code{"static"} (default) or \code{"dynamic"}.}

\item{dawn_times}{POSIXct vector of dawn times, one per unique date in
\code{data}. Required when \code{mode = "dynamic"}, ignored otherwise.}

\item{tz}{Character. IANA timezone name (e.g. \code{"Australia/Perth"}).
When supplied, both \code{data$datetime} and \code{dawn_times} are
converted to this timezone before hour extraction so that the predawn
window is always evaluated in local time. If \code{NULL} (default),
datetimes are used as-is — caller is responsible for ensuring both sides
of the comparison are already in the same timezone.}
}
\value{
A subset of \code{data} restricted to predawn rows.
}
\description{
Subsets a data frame to rows whose \code{datetime} column falls within the
predawn window. Supports static (fixed-clock) and dynamic (dawn-relative)
windows via \code{\link{resolve_predawn_hours}}.
}
