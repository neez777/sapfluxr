% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_heat_pulse_trace_multi}
\alias{plot_heat_pulse_trace_multi}
\title{Plot Heat Pulse Trace - Multi-Method Panel}
\usage{
plot_heat_pulse_trace_multi(
  heat_pulse_data,
  vh_results,
  pulse_id,
  methods = c("HRM", "MHR", "HRMXa", "Tmax_Klu"),
  pre_pulse = 30
)
}
\arguments{
\item{heat_pulse_data}{A heat_pulse_data object from read_heat_pulse_data()}

\item{vh_results}{Results tibble from calc_heat_pulse_velocity()}

\item{pulse_id}{Pulse ID to plot}

\item{methods}{Character vector of exactly 4 methods to show (one per panel).
Default: c("HRM", "MHR", "HRMXa", "Tmax_Klu")}

\item{pre_pulse}{Number of seconds of pre-pulse data to show. Default: 30}
}
\value{
A patchwork object with 4 panels
}
\description{
Creates a 2x2 panel plot showing heat pulse traces for up to 4 methods,
with a shared title and legend. Each panel shows the calculation window
for that specific method.
}
\examples{
\dontrun{
heat_pulse_data <- read_heat_pulse_data("data.txt")
results <- calc_heat_pulse_velocity(heat_pulse_data,
                                     methods = c("HRM", "MHR", "HRMXa", "Tmax_Klu"))

# Create 2x2 panel plot
plot_heat_pulse_trace_multi(heat_pulse_data, results, pulse_id = 1)

# Specify different methods
plot_heat_pulse_trace_multi(heat_pulse_data, results, pulse_id = 1,
                            methods = c("HRM", "HRMXa", "HRMXb", "Tmax_Coh"))
}

}
