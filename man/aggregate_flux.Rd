% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04f_daily_aggregation.R
\name{aggregate_flux}
\alias{aggregate_flux}
\title{Aggregate flux data to arbitrary time periods}
\usage{
aggregate_flux(
  flux_data,
  period = "daily",
  agg_fun = "sum",
  value_col = NULL,
  datetime_col = "datetime"
)
}
\arguments{
\item{flux_data}{Data frame containing flux measurements. Must have a
POSIXct datetime column and at least one numeric value column.}

\item{period}{Character. Aggregation period: \code{"hourly"}, \code{"daily"},
\code{"weekly"}, or \code{"monthly"}. Default: \code{"daily"}.}

\item{agg_fun}{Character. Aggregation function: \code{"sum"} (integrated total,
scales by measurement interval) or \code{"mean"}. Default: \code{"sum"}.}

\item{value_col}{Character. Name of the column to aggregate. If \code{NULL},
auto-detected from common names (\code{"Jv_cm3_cm2_hr"}, \code{"Q_total_L_hr"})
or the first non-datetime numeric column. Default: \code{NULL}.}

\item{datetime_col}{Character. Name of the POSIXct datetime column.
Default: \code{"datetime"}.}
}
\value{
A tibble with columns:
\describe{
\item{period}{POSIXct floor-date for each aggregation window.}
\item{method_label}{Method label, only present if \code{flux_data} contains
a \code{method_label} column.}
\item{aggregated_value}{Aggregated flux for the period.}
\item{n_points}{Number of observations in the window.}
}
}
\description{
Aggregates sap flux density or tree water use to hourly, daily, weekly, or
monthly totals. The sum aggregation scales each observation by the
measurement interval so that rates (per hour) become true period totals.
}
\details{
For \code{agg_fun = "sum"}, each observation is multiplied by the measurement
interval in hours before summing, converting a rate (units/hr) into a
period total (units * hr). The interval is estimated from the median of
consecutive timestamp differences.
}
\examples{
\dontrun{
# After calculating sap flux density
daily <- aggregate_flux(flux_density_data, period = "daily", agg_fun = "sum")
hourly <- aggregate_flux(flux_density_data, period = "hourly", agg_fun = "sum")
monthly <- aggregate_flux(flux_density_data, period = "monthly", agg_fun = "sum")
}

}
\seealso{
Other daily aggregation: 
\code{\link{aggregate_daily}()},
\code{\link{normalise_daily}()}
}
\concept{daily aggregation}
