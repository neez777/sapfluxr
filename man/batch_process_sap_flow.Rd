% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_reporting.R
\name{batch_process_sap_flow}
\alias{batch_process_sap_flow}
\title{Batch Process Multiple Sap Flow Files}
\usage{
batch_process_sap_flow(
  file_paths,
  output_directory,
  methods = c("HRM", "MHR", "DMA"),
  parameters = NULL,
  generate_individual_reports = TRUE,
  generate_consolidated_report = TRUE,
  export_format = "xlsx",
  parallel_processing = FALSE,
  n_cores = NULL,
  error_handling = "warn",
  progress_bar = TRUE
)
}
\arguments{
\item{file_paths}{Character vector of file paths to process}

\item{output_directory}{Character string specifying output directory}

\item{methods}{Character vector of calculation methods to apply to all files}

\item{parameters}{List of calculation parameters (applied to all files)}

\item{generate_individual_reports}{Logical, whether to generate individual reports (default: TRUE)}

\item{generate_consolidated_report}{Logical, whether to generate consolidated report (default: TRUE)}

\item{export_format}{Character string: "csv", "xlsx", "json" (default: "xlsx")}

\item{parallel_processing}{Logical, whether to use parallel processing (default: FALSE)}

\item{n_cores}{Integer, number of cores for parallel processing (default: NULL = auto-detect)}

\item{error_handling}{Character string: "stop", "skip", "warn" (default: "warn")}

\item{progress_bar}{Logical, whether to show progress bar (default: TRUE)}
}
\value{
List containing:
\describe{
\item{processing_summary}{Data frame with processing results for each file}
\item{consolidated_results}{Combined results from all files}
\item{failed_files}{List of files that failed processing}
\item{output_files}{List of all generated output files}
\item{processing_time}{Total processing time}
}
}
\description{
Process multiple sap flow data files in batch with consistent parameters,
automatic report generation, and consolidated output. Ideal for processing
multiple sensors, sites, or time periods.
}
\details{
The batch processing system:
\itemize{
\item Processes files sequentially or in parallel
\item Handles errors gracefully with multiple error handling options
\item Generates individual reports and/or consolidated summary
\item Preserves metadata and processing logs
\item Supports progress tracking and time estimation
\item Creates standardised file naming conventions
}
}
\examples{
\dontrun{
# Process multiple files with default settings
file_list <- list.files("data/", pattern = "\\\\.txt$", full.names = TRUE)
batch_result <- batch_process_sap_flow(
  file_paths = file_list,
  output_directory = "results/",
  methods = c("HRM", "MHR", "DMA")
)

# Parallel processing with custom parameters
batch_result <- batch_process_sap_flow(
  file_paths = file_list,
  output_directory = "results/",
  methods = c("HRM", "DMA"),
  parallel_processing = TRUE,
  n_cores = 4,
  export_format = "csv"
)
}

}
