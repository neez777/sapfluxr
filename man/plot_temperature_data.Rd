% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation_core.R
\name{plot_temperature_data}
\alias{plot_temperature_data}
\title{Plot Raw Temperature Data}
\usage{
plot_temperature_data(
  sap_data,
  pulse_ids = NULL,
  plot_type = "timeseries",
  sensors = "all",
  time_window = NULL,
  facet_by = "none",
  smooth_data = FALSE,
  interactive = FALSE,
  theme_options = NULL
)
}
\arguments{
\item{sap_data}{A sap_data object from read_sap_data()}

\item{pulse_ids}{Optional vector of pulse IDs to plot. If NULL, plots all pulses (default: NULL)}

\item{plot_type}{Character string specifying plot type: "timeseries", "pulse_overlay", "temperature_differences", "diagnostic" (default: "timeseries")}

\item{sensors}{Character vector specifying which sensors to plot: "all", "outer", "inner", c("do", "di", "uo", "ui") (default: "all")}

\item{time_window}{Optional time window to plot. Vector of 2 POSIXct objects (default: NULL)}

\item{facet_by}{Character string for faceting: "pulse", "sensor", "none" (default: "none")}

\item{smooth_data}{Logical whether to add smoothed trend lines (default: FALSE)}

\item{interactive}{Logical whether to create interactive plot (requires plotly) (default: FALSE)}

\item{theme_options}{List of theme options to override defaults (default: NULL)}
}
\value{
A ggplot2 object (or plotly if interactive = TRUE)
}
\description{
Create comprehensive plots of raw temperature measurements from heat pulse sensors
with multiple visualization options and publication-ready formatting.
}
\examples{
\dontrun{
# Basic time series plot
plot_temperature_data(sap_data)

# Pulse overlay comparison
plot_temperature_data(sap_data, plot_type = "pulse_overlay", pulse_ids = 1:5)

# Temperature differences diagnostic
plot_temperature_data(sap_data, plot_type = "temperature_differences")

# Interactive time series
plot_temperature_data(sap_data, interactive = TRUE)
}

}
