% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03c_spacing_correction_plots.R
\name{plot_changepoints_interactive}
\alias{plot_changepoints_interactive}
\title{Plot Interactive Changepoint Detection with Daily Minima}
\usage{
plot_changepoints_interactive(
  daily_min,
  changepoints = NULL,
  segments = NULL,
  proposed_changepoints = NULL,
  vh_data = NULL,
  title = "Daily Minimum Velocities with Changepoints",
  show_baseline_values = TRUE,
  show_original_data = FALSE
)
}
\arguments{
\item{daily_min}{Data frame with columns \code{date} and \code{min_value}}

\item{changepoints}{Vector of changepoint dates (Date class), or NULL for no changepoints.
These are "confirmed" changepoints that will have baselines calculated.}

\item{segments}{Optional data frame of segments with baseline values. If NULL and
changepoints are provided, segments will be auto-generated from changepoints.}

\item{proposed_changepoints}{Optional vector of proposed/detected changepoint dates that
haven't been confirmed yet. Shown in orange without baselines.}

\item{vh_data}{Optional full velocity data frame for overlay (must have datetime and Vh_cm_hr)}

\item{title}{Character, plot title (optional)}

\item{show_baseline_values}{Logical, whether to show baseline values for each segment (default: TRUE)}

\item{show_original_data}{Logical, whether to overlay original velocity data (default: FALSE)}
}
\value{
A plotly object
}
\description{
Creates an interactive plotly visualization showing daily minimum velocities
with detected changepoints as vertical dashed lines. Supports zooming and
panning for visual assessment of baseline shifts.
}
\examples{
\dontrun{
# Calculate daily minima
daily_min <- calculate_daily_minima(vh_results)

# Detect changepoints
cpt_result <- detect_changepoints(daily_min)

# Get segments with baselines
segments <- extract_segment_baselines(cpt_result)

# Create interactive plot
plot_changepoints_interactive(
  daily_min = cpt_result$daily_min_with_segments,
  changepoints = cpt_result$changepoints,
  segments = segments
)
}
}
\seealso{
Other spacing correction plots: 
\code{\link{plot_burgess_coefficients}()},
\code{\link{plot_k_estimation_summary}()},
\code{\link{plot_spacing_correction_comparison}()},
\code{\link{plot_spacing_correction_report}()},
\code{\link{plot_symmetry_check}()},
\code{\link{plot_temperature_traces}()},
\code{\link{plot_zero_flow_periods}()}
}
\concept{spacing correction plots}
