% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_processing_pipeline.R
\name{processing_pipeline}
\alias{processing_pipeline}
\alias{process_sap_data}
\title{Unified Sap Flow Data Processing Pipeline}
\usage{
process_sap_data(
  sap_data,
  methods = NULL,
  parameters = NULL,
  probe_config = NULL,
  wood_properties = NULL,
  quality_control = TRUE,
  export_results = FALSE,
  output_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{sap_data}{Sap flow data object from read_sap_data() or read_multiple_sap_data()}

\item{methods}{Character vector of methods to calculate. If NULL, uses recommended methods}

\item{parameters}{List of calculation parameters. If NULL, uses defaults}

\item{quality_control}{Logical, whether to perform quality control (default TRUE)}

\item{export_results}{Logical, whether to export results to file (default FALSE)}

\item{output_file}{Character, output file path if export_results = TRUE}

\item{verbose}{Logical, whether to print processing information (default TRUE)}
}
\value{
List containing:
\describe{
\item{velocity_results}{Data frame with calculated velocities from calc_heat_pulse_velocity()}
\item{quality_assessment}{Quality assessment results}
\item{probe_config}{Detected probe configuration}
\item{method_recommendations}{Recommended methods and parameters}
\item{processing_summary}{Summary of processing steps}
\item{diagnostics}{Detailed diagnostic information}
}
}
\description{
This file implements the main processing pipeline functions that were missing
from the sapFluxR package. These work with the existing calc_hrmx and calc_dma
functions that are already implemented.
}
\details{
The processing pipeline:
\enumerate{
\item Validates input data using existing validate_sap_data()
\item Detects probe configuration using existing detect_probe_config()
\item Performs comprehensive quality assessment
\item Recommends optimal methods based on configuration and data quality
\item Calculates velocities using existing calc_heat_pulse_velocity()
\item Applies quality control and provides comprehensive diagnostics
}
}
\examples{
\dontrun{
# Basic processing with automatic method selection
results <- process_sap_data(sap_data)
print(results$processing_summary)

# Process with specific methods
results <- process_sap_data(
  sap_data,
  methods = c("HRM", "MHR", "DMA"),
  quality_control = TRUE
)
}

}
\seealso{
\code{\link{calc_heat_pulse_velocity}}, \code{\link{assess_data_quality}}
}
\concept{processing
Process Sap Flow Data with Automatic Method Selection

Main pipeline function that processes sap flow data with automatic probe
configuration detection, method compatibility checking, and quality assessment.
Uses the existing calc_heat_pulse_velocity() function but adds intelligent
method selection and comprehensive reporting. Handles both single-tree and
multi-tree data objects.}
