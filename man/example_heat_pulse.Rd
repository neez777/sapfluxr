% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{example_heat_pulse}
\alias{example_heat_pulse}
\title{Example heat pulse data}
\format{
A list of class \code{heat_pulse_data} with the elements:
\describe{
\item{measurements}{A tibble of 67,060 per-second temperature readings with
columns \code{datetime} (POSIXct, UTC), \code{pulse_id} (integer), and the four
probe temperatures \code{do}, \code{di}, \code{uo}, \code{ui} (numeric, degrees Celsius) for
the outer/inner downstream and outer/inner upstream sensors.}
\item{diagnostics}{A tibble with one row per pulse (480 rows) of logger
diagnostics: \code{pulse_id}, \code{datetime}, battery voltage/current/temperature,
external voltage/current, and \code{is_missing_pulse}.}
\item{missing_pulse_info}{Details of any gaps detected in the pulse
sequence.}
\item{metadata}{A list describing the import: file name, detected format,
pulse and measurement counts, and package version.}
\item{gap_detection}{Summary of the pulse-sequence gap check.}
\item{validation}{The data-quality validation result, including \code{valid}
(logical) and any issues or warnings.}
}
}
\source{
A 10-day slice of unpublished thesis field data (G. Joyce),
\emph{Eucalyptus marginata}, Perth, Western Australia. Raw temperatures only;
no derived ratios.
}
\usage{
example_heat_pulse
}
\description{
A parsed heat pulse dataset covering a 10-day window (2024-11-01 to
2024-11-10, UTC) from a single ICT SFM1 sap flow sensor deployed on a
\emph{Eucalyptus marginata} (jarrah) tree near Perth, Western Australia. It is the
object returned by \code{\link[=read_heat_pulse_data]{read_heat_pulse_data()}} applied to the bundled raw file
\code{Sample_HeatPulse_Data.txt} (see \link{sapfluxr_example_files}), and is intended
for examples, vignettes, and testing the downstream pipeline (heat pulse
velocity, corrections, sDMA, flux density, aggregation).
}
\details{
Pulses are recorded every 30 minutes. The window contains one gap in the
pulse sequence, which the importer flags and back-fills with a placeholder
row, so the dataset also demonstrates gap detection.
}
\examples{
data(example_heat_pulse)
str(example_heat_pulse$measurements)
range(example_heat_pulse$measurements$datetime)
}
\seealso{
\link{example_weather}, \link{sapfluxr_example_files}, \code{\link[=read_heat_pulse_data]{read_heat_pulse_data()}}
}
\keyword{datasets}
